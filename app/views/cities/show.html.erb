<div class="container my-4">

  <!-- City Title -->
  <h1 class="mb-4 fw-bold"><%= @city.name %> - Itinerary 🚶🏻‍➡️📸</h1>

  <div class="row">

    <!-- Itinerary Column -->
    <div class="col-md-8">
      <div>
        <div class="card-body">
          <% if @city.itinerary.present? %>
            <div class="itinerary-content">
              <%= raw(@city.itinerary.to_s) %>
            </div>
          <% else %>
            <p class="text-muted">Itinerary will be generated soon.</p>
          <% end %>
        </div>
      </div>
    </div>

    <!-- Trip Chats Column -->
    <div class="col-md-4">
      <div class="card shadow-sm mb-4">
        <div class="card-header bg-light">
          <h4 class="mb-0">Trip Chats</h4>
        </div>
        <div class="card-body">
          <% @city.chats.each do |chat| %>
            <%= link_to(chat_path(chat), class: "text-decoration-none") do %>
              <div class="border p-3 rounded mb-2 hover-shadow">
                <%= chat.title.presence || "Untitled Chat" %>
              </div>
            <% end %>
          <% end %>
        </div>
      </div>
      <div class="mb-4">
      <%= simple_form_for @city do |f| %>
  <div class="postit">
    <div id="ntitle">
    <%= f.input :notes, label: "Notes", as: :text, input_html: { rows: 5 } %>
    </div>
    <div class="button-container">
      <%= f.button :submit, "Save", class: "saveNbtn btn btn-success" %>
    </div>
  </div>
<% end %>

      </div>
    </div>
  </div>
</div>
